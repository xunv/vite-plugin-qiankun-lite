---
'@xunv/vite-plugin-qiankun-lite': minor
---

Support Vite 8 by widening the `vite` peer dependency range to `>= 4 < 9`, and fix TypeScript type resolution (TS7016) under `node16`/`nodenext` module resolution by adding a `types` condition to the package `exports`.
